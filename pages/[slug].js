import React from 'react'

const Results = ({ posts }) => {
    return (
        <div>
            page
        </div>
    )
}

export default Results

export const getStaticProps = async () => {
    
}
